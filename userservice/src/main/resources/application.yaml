spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/memento_user_db
    username: root
    password: Admin@2026

  jpa:
    hibernate:
      ddl-auto: update

server:
    port: 8081